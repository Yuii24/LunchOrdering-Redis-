{{> order-tabs route='restaurants'}}
<br>
<a href='/admin/restaurants/create'>
  <button type="button" class="btn btn-primary">新增餐廳</button>
</a>
<br />
<br />
<table class="table align-middle">
  <thead class="thead-dark">
    <tr>
      <th scope="col">Name</th>
      <th scope="col"></th>
    </tr>
  </thead>
  <tbody>
    {{#each restaurants}}
    <tr>
      <td>{{this.name}}</td>
      <td>
        <button type="button" class="btn btn-link">
          <a class="btn btn-link link-primary" href="/restaurants/{{this.id}}">
            查看餐廳資料
          </a>
        </button>
        <button type="button" class="btn btn-link">
          <a class="btn btn-link link-primary" href="/admin/restaurants/{{this.id}}/edit">
            編輯餐廳
          </a>
        </button>
        {{!-- 這個功能暫時不開放 --}}
        {{!-- <form action="/admin/restaurants/{{this.id}}?_method=DELETE" method="POST" style="display: inline;">
          <button type="submit" class="btn btn-link link-danger"
            onclick="return confirm(`確定要刪除 {{ this.name }} 嗎？`)">刪除餐廳</button>
        </form> --}}
      </td>
    </tr>
    {{/each}}
  </tbody>
</table>